import React from 'react';
import './Spinner.css';

const Spinner = () => {
    return (
        <div className="spinner">
            <p>CARGANDO</p>
        </div>
      );
}
 
export default Spinner;